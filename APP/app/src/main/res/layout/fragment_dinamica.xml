<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/fondoVentanas">

    <EditText
        android:id="@+id/editTextDinamica"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="90dp"
        android:layout_marginRight="20dp"
        android:background="@android:color/white"
        android:gravity="top"
        android:hint="¿Qué deseas consultar?"
        android:inputType="text|textMultiLine"
        android:maxLines="8"
        android:padding="10dp" />

    <Button
        android:id="@+id/botonDinamica"
        android:layout_below="@+id/editTextDinamica"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/circular_button"
        android:drawableTop="@drawable/ic_google_play2"
        android:paddingTop="7dp"
        android:paddingLeft="10dp"
        android:textColor="#fff" />

</RelativeLayout>